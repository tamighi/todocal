import { Container } from "@/atoms";
import { DayComponent } from "@/components";

const DayScreen = () => {
  return (
    <Container margin="xl">
      <DayComponent />
    </Container>
  );
};

export default DayScreen;
